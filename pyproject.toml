[project]
name = "thunderstorm-forecasting"
version = "1.0.0"
description = "Next-day thunderstorm prediction using atmospheric indices and QDA."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "streamlit",
    "pandas",
    "numpy",
    "scikit-learn",
    "imbalanced-learn",
]